

import { CommonListMenuItem } from './common-list-menu-item';
export class CommonListMenu {
    visibleOnDesk: boolean = false; // show mat menu when view on desktop (large screen).
 // show mat menu when view on desktop (large screen).
    visibleOnMob: boolean = false;

    menuItems: CommonListMenuItem[] = [];
}